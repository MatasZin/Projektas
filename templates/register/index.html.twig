{% extends 'base.html.twig' %}

{% block content %}
    {{ form_start(form, { 'attr': { 'class' : 'form-centered' } }) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <p>* - is required</p>
    {{ form_end(form) }}
{% endblock %}