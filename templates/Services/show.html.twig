{% extends 'base.html.twig' %}
{% block content %}
    <h1>{{ service.title }}</h1>
    <p>{{ service.description }}</p>
    <hr>
    <a href="{{ path('Services') }}" class = modern>Go Back</a>
{% endblock %}